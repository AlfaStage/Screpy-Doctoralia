# Doctoralia Scraper - Environment Variables

# BrightData Proxy (opcional - proxy pago de alta qualidade)
# Preencha com suas credenciais do BrightData
BRIGHTDATA_HOST=brd.superproxy.io
BRIGHTDATA_PORT=33335
BRIGHTDATA_USERNAME=brd-customer-xxx-zone-xxx
BRIGHTDATA_PASSWORD=your_password_here

# Se esta variável estiver preenchida, o BrightData será usado como proxy prioritário
# Deixe vazio ou remova para usar apenas proxies gratuitos

# API Configuration
# API Key for REST API authentication (auto-generated if empty on first run)
API_KEY=
# Server port (default: 3000)
PORT=3000
